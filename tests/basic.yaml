tests:
  - name: "Basic sanity"
    task: "What is 2 + 2?"
    expect:
      contains: "4"

  - name: "Weather skill activates"
    task: "What's the weather in Madrid right now?"
    expect:
      contains: "Madrid"
